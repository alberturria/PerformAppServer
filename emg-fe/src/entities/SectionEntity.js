export default class SectionEntity{
    constructor (start, end, values, waveId) {
        this.start = start;
        this.end = end;
        this.values = values;
        this.waveId = waveId;        
    }
}